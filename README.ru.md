# Componenta Iterator

Утилиты для итераторов: повторный обход одноразовых источников, независимые курсоры, обратный обход, обход строк и преобразование итератора в массив.

Используйте пакет, когда библиотеке нужно поведение итераторов без зависимости от collection framework.

## Установка

```bash
composer require componenta/iterator
```

## Связанные пакеты

| Пакет | Зачем нужен здесь |
|---|---|
| `componenta/stream-iterator` | Для больших PSR-7 потоков лучше использовать stream-iterator: он держит в памяти только текущий chunk. |
| `componenta/arrayable` | Некоторые итераторы раскрывают `toArray()` и могут использоваться как arrayable-объекты. |

## ReplayableIterator

`ReplayableIterator` оборачивает массивы, итераторы, iterator aggregates и generators. Он лениво кеширует уже прочитанные entries, поэтому одноразовый источник можно повторно обходить, не материализуя его полностью заранее.

```php
use Componenta\Stdlib\ReplayableIterator;

$iterator = new ReplayableIterator((function () {
    yield 'a' => 1;
    yield 'b' => 2;
})());

$iterator->toArray(preserveKeys: true); // ['a' => 1, 'b' => 2]
```

Объект сохраняет обычный одно-курсорный API `Iterator`. Если два потребителя должны проходить один источник независимо или одновременно, используйте отдельные курсоры:

```php
$first = $iterator->cursor();
$second = $iterator->cursor();

$first->next();
// $second по-прежнему находится на собственной первой записи.
```

У каждого cursor своя позиция; общим остаётся только ленивый replay-cache. Внутренний кеш сохраняет повторяющиеся и `null`-ключи исходного iterator. Вызов `count()` или `toArray()` принудительно проходит весь источник.

## Обратный обход

`ReverseIterator` итерирует iterable в обратном порядке. Он материализует значения внутри, поэтому рассчитан на конечные последовательности.

`ArrayListReverseIterator` — небольшой reverse iterator для list arrays.

## StringIterator

Пакет требует расширение PHP `mbstring`. `forward()` достигает того же состояния конца строки, что и `next()`; нулевое смещение сохраняет курсор, а движение вперёд после завершённого обхода оставляет его завершённым.

`StringIterator` итерирует строку с поддержкой кодировки и методами управления курсором:

- `moveTo()`
- `forward()`
- `backward()`
- `read()`
- `remaining()`
- `peek()`

## Преобразование в массив

`IteratorToArray` задаёт интерфейс `toArray()` для iterator-классов, которые могут материализовать своё содержимое.

## Разработка

```bash
composer install
composer test
```

CI проверяет Composer metadata, синтаксис PHP и запускает Pest на PHP 8.4 и 8.5.

## Память

Replayable и reverse iterators меняют память на возможность повторного или обратного обхода. Они подходят для конечных последовательностей. Для больших PSR-7 потоков используйте `componenta/stream-iterator`, который держит только текущий chunk.


`replay()` — совместимое имя `cursor()`: каждый вызов создаёт независимый курсор.
Если источник выбросил исключение, дальнейшие чтения и продвижение ранее созданных
курсоров повторно выбрасывают то же исключение. Ошибка не превращается в успешный
результат с неполным набором элементов.

`StringIterator::forward()` принимает неотрицательное число шагов и позволяет
перейти в позицию конца строки: `isEnd()` возвращает `true`, `current()` — `null`.
Нулевой шаг сохраняет позицию, слишком большой шаг останавливается в конце строки.
Вызов `backward()` из конца возвращает последний символ. Отрицательные шаги
вызывают `InvalidArgumentException`.
